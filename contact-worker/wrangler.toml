name = "emailworker"
main = "index.ts"
compatibility_date = "2024-03-01"

[vars]
# Add non-secret vars here if needed
# Secret vars (CYPHEX_MAIL_API_KEY) should be set using: wrangler secret put CYPHEX_MAIL_API_KEY
